import React from "react";
import Logo from "./Logo";

const Footer: React.FC = () => (
  <footer className="h-36 flex justify-center items-center  bg-header-900">
    <Logo />
  </footer>
);

export default Footer;
